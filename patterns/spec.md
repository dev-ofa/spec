## patterns

### 状态
Draft

### CRUD
- 资源以名词复数形式命名
- Create 与 Update 必须返回统一 Wrapper
- Delete 建议返回被删除资源的 id

### 选主
- 必须具备主从切换与主失效自动恢复能力
- 必须暴露当前主节点的只读查询接口
- 选主状态变更必须记录日志

### 异步 Worker
- 任务必须具备幂等性设计
- 失败任务必须支持重试与死信
- 任务处理必须记录开始与结束日志

### 唯一 ID
- 必须保证全局唯一且趋势递增
- 必须支持多节点并发生成
- 推荐使用雪花算法或同等能力方案
