# AGENTS.md

## 定位
- 本仓库用于维护规范文档，不承载业务实现代码。
- Agent 在本仓库中的职责是编写、修改、整理和对齐 spec、guide、template、ADR 等文档。
- 本文件约束 Agent 的工作方式，不定义具体领域规则；具体规则以各文档正文为准。

## 基本原则
- 不臆造规则。未被明确提出或确认的内容，不要写成强制要求。
- 不混淆强度。明确区分“必须 / 应当 / 可以”，不要把建议写成必须。
- spec 可以定义为保证跨语言、跨实现、跨团队协作一致性所必需的具体约束，例如协议字段、header 名、错误类型、状态机、加载顺序、兼容性边界。
- 仅对某种 SDK、框架、目录布局、部署方式或推荐工程实践成立的内容，应放在 guide、example 或 template，不应写成通用 spec。
- 不重复定义同一规则。已有规则优先引用，不要在多个文档中各写一版。
- 不忽略文档边界。先判断内容属于 spec、guide、template 还是 ADR。

## 写作要求
- 每份 spec 应优先说明：目标、适用范围、非目标。
- 当字段名、协议格式、加载顺序、失败类型等是互操作或治理前提时，应在 spec 中明确写出，不因“过于具体”而回避。
- 关键规则应尽量可判断“合规 / 不合规”，避免空泛表述。
- 示例仅用于帮助理解，不替代正文规则。
- 同一概念在全仓库中保持统一术语。
- 若规则存在例外、前提或适用条件，应明确写出。

## 修改要求
- 修改前先阅读相关文档，理解现有术语、边界和引用关系。
- 修改时检查是否影响其他 spec、guide、template 或 ADR。
- 如果变更会改变原有含义，应明确标注影响范围和待确认问题。
- 如果发现冲突或空白，不要强行补齐，应显式指出。

## 工作流程
1. 识别任务类型：新增、修改、对齐或审查。
2. 确认目标文档类型和边界。
3. 阅读相关文档，避免重复和冲突。
4. 做最小必要修改，不扩大范围。
5. 说明本次变更、影响范围和待确认事项。

## 输出要求
提交结果时应说明：
- 本次修改的目标
- 受影响的文档
- 是否新增或修改了术语、规则或边界
- 是否存在冲突、歧义或待确认问题

## 禁止事项
- 不要把仅对某种实现成立的方案伪装成通用规范。
- 不要把推荐项写成强制项。
- 不要只改当前文件而忽略相关文档一致性。
- 不要用示例、FAQ 或 checklist 替代规范正文。
- 不要在依据不足时新增强约束。
